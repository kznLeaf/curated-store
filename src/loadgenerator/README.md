# Load Generator

The project includes a built-in load generator based on [Locust](https://locust.io/), a Python-based load testing tool. The load generator simulates realistic user behaviors to test the application under various load conditions.

See: https://docs.locust.io/en/stable/running-without-web-ui.html#running-without-web-ui